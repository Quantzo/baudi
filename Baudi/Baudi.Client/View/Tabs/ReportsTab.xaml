<UserControl x:Class="Baudi.Client.View.Tabs.ReportsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:converters="clr-namespace:Baudi.Client.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumConventer" />
    </UserControl.Resources>
    
    <Grid Style="{StaticResource MainGrid}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>




        <Label Grid.Row="0" Grid.Column="0" Margin="5" HorizontalContentAlignment="Right"
               VerticalContentAlignment="Center" Content="Od" />
        <DatePicker Grid.Row="0" Grid.Column="1"  Margin="5" HorizontalAlignment="Center" VerticalContentAlignment="Center" SelectedDate="{Binding
        Path=DateFrom,
        Mode=TwoWay,
        TargetNullValue={x:Static System:DateTime.Now}}" />

        <Label Grid.Row="1" Grid.Column="0" Margin="5" HorizontalContentAlignment="Right"
               VerticalContentAlignment="Center" Content="Do" />
        <DatePicker Grid.Row="1" Grid.Column="1"  Margin="5" HorizontalAlignment="Center"  VerticalContentAlignment="Center" SelectedDate="{Binding
        Path=DateTo,
        Mode=TwoWay,
        TargetNullValue={x:Static System:DateTime.Now}}" />

        <Label Grid.Row="2" Grid.Column="0" Margin="5" HorizontalContentAlignment="Right"
               VerticalContentAlignment="Center" Content="Rodzaj raportu" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="5" IsSynchronizedWithCurrentItem="True"
                  ItemsSource="{Binding ReportTypeValues }" SelectedItem="{Binding ReportType }">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumConventer}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Label Grid.Row="3" Grid.Column="0" Margin="5" HorizontalContentAlignment="Right"
               VerticalContentAlignment="Center" Content="Raport dla budynku"  />
        <ComboBox Grid.Row="3" Grid.Column="1" Margin="5" IsSynchronizedWithCurrentItem="True" IsEnabled="{Binding IsBuildingReport}"
                  ItemsSource="{Binding BuildingsList}" SelectedValue="{Binding SelectedBuilding}"
                  DisplayMemberPath="NotificationTargetID"/>

        <Button Grid.Row="4" Grid.Column="1" Margin="5" Command="{Binding ButtonGenerate}" Content="Generuj" />

    </Grid>
</UserControl>